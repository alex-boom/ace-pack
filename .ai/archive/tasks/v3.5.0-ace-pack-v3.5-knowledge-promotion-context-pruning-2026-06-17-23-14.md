# Task State

## Lifecycle & Meta

### Feature Name
ACE Pack v3.5 Knowledge Promotion & Context Pruning

### Lifecycle
Status: active
Version: v3.5.0
Task Tier: large
Design Review Required: yes
Friction Encountered: no
Current Phase: Complete
Next Autonomous Action: No further autonomous action; task is complete.
Started: 2026-06-17 20:45
Ready For Archive: no

### Goal
Add explicit knowledge promotion context and deterministic active-log rotation so
ACE memory can stay useful without hidden AI/API calls or semantic processing in
scripts.

### Current Status
- [x] Implemented `ace hub distill` / `promote`.
- [x] Implemented `ace archive`.
- [x] Updated shipped templates, docs, package version, and tests.
- [x] Ran full validation, fake-project smoke, ACE check/classify, and npm
  payload guard.

### Affected Areas
- `scripts/ace-hub*.mjs`
- `scripts/ace-cli.mjs`
- `scripts/ace-archive.mjs`
- `scripts/agent-memory-templates.mjs`
- `scripts/ace-uninstall-utils.mjs`
- `README.md`, `README.npm.md`, `docs/schema-compatibility.md`
- `package.json`
- `tests/**`

### Constraints
- Keep ACE local-first, zero-dependency, and zero hidden AI/network calls.
- Do not archive `decisions.md` in v3.5.
- Preserve compatibility with existing memory files.
- Keep script files under the 400-line guard.
- Do not edit repo-local root `AGENTS.md` or `CLAUDE.md` to change product behavior.

### Acceptance Criteria
- [x] `ace hub distill` and alias `promote` output knowledge promotion context.
- [x] Distill handles optional conventions, empty resolved reflections, and
  missing or broken `## Resolved` formatting with the specified fallback
  instruction.
- [x] `ace archive` rotates only work-log and reflection-log with `--max-lines`
  and `--dry-run`.
- [x] Fresh active logs contain clickable relative archive links.
- [x] Same-day repeated archives do not overwrite existing archive files.
- [x] Router, install sync, docs, schema tests, and full validation pass.

### Completion Checklist
- [x] Goal completed
- [x] Future agent context preserved
- [x] Verification recorded
- [x] Publish/deploy decision recorded when relevant
- [x] Extra docs updated only where changed
- [x] `ace:validate` and release checks passed

## Business Value & Approach

### Business Value / Product Alignment
Knowledge promotion lets agents turn resolved friction into durable project
rules, while archive rotation keeps daily context small and focused. This
reduces repeated mistakes without adding runtime orchestration or provider
integration.

### Technical Approach
Option 1:
- Add semantic summarization into `ace archive`. This would be convenient but
  violates ACE's no hidden AI calls and deterministic local-first model.

Option 2:
- Add `hub distill` as a prompt generator for explicit agent work, and keep
  `ace archive` as a mechanical Markdown rotation command.

Chosen Approach:
- Use Option 2. It separates intelligence from mechanics, keeps scripts
  dependency-free, and preserves human-readable Markdown as the data bus.

### Edge Cases & Red Teaming
- Failure mode: a human breaks the `reflection-log.md` headings. Mitigation:
  distill falls back to the full file with a strict instruction to focus only on
  `## Resolved` and ignore `## Unresolved`.
- Failure mode: archive runs multiple times on the same date. Mitigation: archive
  filenames get `-2`, `-3`, etc. suffixes without overwriting.
- Failure mode: log rotation removes section headings needed by checks.
  Mitigation: fresh `reflection-log.md` keeps `## Unresolved` and `## Resolved`.

## Changed Files / Diff

[scripts/ace-hub-distill.mjs, scripts/ace-hub.mjs, scripts/ace-hub-modes.mjs]
- Added `distill` / `promote` hub mode with phase/action metadata, project
  conventions context, resolved-reflection extraction, empty-resolved handling,
  and the strict fallback instruction for missing or broken `## Resolved`.

[scripts/ace-archive.mjs, scripts/ace-cli.mjs, scripts/ace-uninstall-utils.mjs]
- Added deterministic `ace archive` routing, CLI flags, dry-run behavior,
  same-day suffixes, fresh active log links, and managed script installation.

[scripts/agent-memory-templates.mjs]
- Added context hygiene rules for `hub distill`, `archive`, `.ai/archive/**`
  deep-history treatment, and the v3.5 decision not to archive `decisions.md`.

[README.md, README.npm.md, docs/schema-compatibility.md, package.json]
- Documented v3.5 schema and command surfaces, updated command tables, and
  bumped the package version to `3.5.0`.

[tests/**]
- Added/updated coverage for hub distill payloads, archive rotation, router
  mappings, installed script sync, shipped instructions, and schema docs.

## Handoff & Next Steps

### Last Update
2026-06-17 23:12

### What Was Done
- Implemented the v3.5 knowledge promotion and context pruning pipeline.
- Kept semantic distillation in explicit agent prompts and archive rotation
  deterministic.

### Current State
- Complete and ready for maintainer review.

### Quality Review
Product Alignment:
- v3.5 directly supports self-healing ACE memory without adding hidden LLM calls
  or runtime orchestration.

Architecture:
- Distill follows the existing hub payload-builder pattern; archive is a separate
  deterministic CLI with no semantic summary logic.

Security:
- No dependencies, network calls, credentials, provider integrations, or
  background schedulers were added.

Code Quality:
- New behavior is covered by focused tests and kept in isolated script modules
  under the line-limit guard.

### Next Steps
- Maintainer review, then run the npm release flow when ready.

### Known Issues
- None known.

### Verification
- `npm.cmd test -- tests/ace-hub.test.ts tests/ace-archive.test.ts tests/ace-cli.test.ts tests/agent-memory.test.ts tests/install-agent-memory-pack.test.ts tests/schema-compatibility.test.ts` passed.
- `npm.cmd run typecheck` passed.
- `npm.cmd run lint` passed.
- `npm.cmd run test` passed: 17 files / 146 tests.
- `npm.cmd run ace:validate` passed: typecheck, line-limit lint, and Vitest.
- `npm.cmd run smoke:fake-project` passed for JS and non-JS fake projects.
- `npm.cmd run ace -- check` passed.
- `npm.cmd run ace -- classify` passed; current diff is large and design review
  is required.
- `npm.cmd run check:npm-payload` passed; 46 packed files checked.
- `git diff --check` passed.

### Notes
- NPM publish: required because shipped scripts/templates/docs and package
  version changed.
