# Task State

## Lifecycle & Meta

### Feature Name
ACE v3.5.1 Safe Migration Patch and Platform Upgrade

### Lifecycle
Status: complete
Version: v3.5.1
Task Tier: large
Design Review Required: yes
Friction Encountered: no
Current Phase: Complete
Next Autonomous Action: No further autonomous action; task is complete.
Started: 2026-06-19 15:05
Ready For Archive: yes

### Goal
Fix ACE legacy task-state migration so real Markdown checklists and bracketed
content are preserved, then safely migrate `D:\All\alex-work\platform` to the
patched v3 canonical ACE layout without losing existing `.ai` memory.

### Current Status
- [x] Migration issue reproduced on a temporary copy of `platform`.
- [x] User approved patch-first, canonical-only cleanup, and SaaS preset apply.
- [x] Patch ACE placeholder detection and regression tests.
- [x] Validate ACE package and npm payload dry-run.
- [x] Back up and migrate `platform`.
- [x] Validate platform ACE state and review diffs.

### Affected Areas
- `scripts/ai-markdown-utils.mjs`
- `scripts/ai-memory-utils.mjs`
- `tests/schema-compatibility.test.ts`
- `package.json`
- `D:\All\alex-work\platform` ACE scripts, memory, `AGENTS.md`, and `CLAUDE.md`

### Constraints
- Preserve ACE local-first, zero-dependency, no hidden AI/network calls.
- Do not overwrite or lose `platform` legacy `.ai` memory; back it up before pruning.
- Keep `platform` on `dev` and preserve project-owned `pnpm ace:validate`.
- Keep heavy `.ai/graphql-schemas`, `.ai/logs`, and `.ai/tmp` out of daily ACE context.
- NPM publish was required and completed because shipped migration behavior
  changed.

### Acceptance Criteria
- Legacy `Current Status` and `Completion Checklist` checkbox sections migrate into `.ai/state/task-state.md`.
- Real bracketed content is not treated as placeholder-only content.
- ACE package version is bumped to `3.5.1`.
- ACE tests, typecheck, lint, payload check, and npm dry-run pass.
- `platform` uses patched ACE v3 canonical paths and router commands.
- `platform` legacy root memory files are pruned only after backup and canonical copy verification.
- Final handoff states platform deploy and ACE npm publish decisions.

### Completion Checklist
- [x] Goal completed
- [x] Future agent context preserved
- [x] Verification recorded
- [x] Publish/deploy decision recorded
- [x] Platform migration backup path recorded
- [x] `ace finish` passed and generated reports

## Business Value & Approach

### Business Value / Product Alignment
This patch protects installed repositories from losing meaningful legacy task
checklists during v3 migration, which is essential for safe ACE upgrades in real
projects with existing `.ai` history.

### Technical Approach
Option 1:
- Install ACE v3.5.0 into `platform` and manually repair the migrated task-state.
  This is fast but leaves the product migration bug for every other user.

Option 2:
- Fix ACE placeholder detection first, add regression coverage, release as
  v3.5.1, and then migrate `platform` with the patched local package.

Chosen Approach:
- Use Option 2. The bug is in shipped migration behavior, so the product should
  be corrected before using it for a high-value repository migration.

### Edge Cases & Red Teaming
- Failure mode: placeholder detection becomes too loose and preserves template
  sections as meaningful content. Mitigation: keep explicit placeholder labels
  and add tests around checklist preservation, not broad removal of placeholder
  handling.
- Failure mode: platform cleanup removes the only copy of legacy memory.
  Mitigation: create an external backup plus ACE migration backup before pruning
  root aliases, then verify canonical files contain expected content.
- Failure mode: platform instructions still mention old paths after migration.
  Mitigation: manually update `AGENTS.md` and `CLAUDE.md`, then grep for stale
  command/path references.

## Changed Files / Diff

[ACE package]
- Fixed placeholder detection for real Markdown checklist and bracketed content.
- Added migration regression coverage for legacy `Current Status` and
  `Completion Checklist`.
- Bumped `package.json` to `3.5.1`.

[Platform]
- Backed up legacy ACE state, installed patched ACE, applied the
  `next-trpc-drizzle-saas` preset, migrated canonical memory, and pruned root
  aliases after verification.

## Handoff & Next Steps

### Last Update
2026-06-19 15:20

### What Was Done
- Reproduced and fixed the v3.5.0 migration issue.
- Validated the ACE package and npm dry-run.
- Migrated `platform` to canonical ACE v3 layout using the patched local
  package.

### Current State
- ACE patch, `platform` migration, and npm publish are complete.

### Quality Review
Product Alignment:
- The fix improves safe upgrades for real repositories with existing ACE memory.

Architecture:
- The change stays inside deterministic Markdown helpers, migration utilities,
  templates, and regression tests.

Security:
- No new dependencies, network calls, providers, or hidden automation were
  added.

Code Quality:
- Regression coverage pins legacy checklist preservation and alias promotion
  behavior.

### Next Steps
- Review and commit both repository diffs.

### Known Issues
- None known.

### Verification
- `npm.cmd run ace -- classify` ran before edits; it reported `small` only
  because no diff existed yet.
- `npm.cmd test -- tests/schema-compatibility.test.ts` passed.
- `npm.cmd run typecheck` passed.
- `npm.cmd run lint` passed.
- `npm.cmd run test` passed.
- `npm.cmd run release:npm:dry` passed, including payload guard, `npm pack`
  dry-run, and `npm publish --dry-run`.
- Before publish, `npm.cmd view ace-pack version` returned `3.5.0`, so
  `3.5.1` was not already published.
- After maintainer publish, `npm.cmd view ace-pack version` returned `3.5.1`;
  registry `time` reports `3.5.1` published at `2026-06-19T12:29:24.608Z`.
- `platform` backup created at
  `D:\All\alex-work\backups\platform-ace-pre-v351-20260619-150734`.
- `platform` `pnpm.cmd ace check`, `pnpm.cmd ace hub --mode start`,
  `pnpm.cmd ace:validate`, and `pnpm.cmd ace finish` passed.
- `npm.cmd run ace -- check` and `npm.cmd run ace -- finish` passed in the ACE
  package repository.

### Notes
- NPM publish: completed for `ace-pack@3.5.1`.
- Platform deploy: not required; only ACE workflow/memory files changed.
